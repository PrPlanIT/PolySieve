# ------------------------------------------------------------------------------
# GENERATED / ENFORCED BY STAGEFREIGHT GOVERNANCE
# Source repo: PrPlanIT/MaintenancePolicy
# Source ref: 3f7d914f8c613003dc456dd939f81ac98e0b5b69
# Profile: prplanit-go
# This .stagefreight.yml is governed, not purely local.
# To disable governance, detach from the control repo workflow.
# Manual changes may be overwritten by reconciliation.
# ------------------------------------------------------------------------------

version: 1
vars:
  github_org: PrPlanIT
  github_repo: PolySieve
  gitlab_group: PrPlanIT
  org: prplanit
  repo: polysieve
builds:
  bin:
    args:
      - -ldflags
      - -s -w -X github.com/PrPlanIT/PolySieve/internal/cli.version={version} -X github.com/PrPlanIT/PolySieve/internal/cli.commit={sha} -X github.com/PrPlanIT/PolySieve/internal/cli.buildDate={date}
    builder: go
    env:
      CGO_ENABLED: "0"
    from: ./cmd/polysieve
    kind: binary
    output: polysieve
    platforms:
      - linux/amd64
      - linux/arm64
  preset: preset/build-docker.yml
scribe:
  preset: preset/scribe-go-docker.yml
lint:
  preset: preset/lint-flagships.yml
security:
  preset: preset/security.yml
dependency:
  preset: preset/dependency.yml
commit:
  preset: preset/commit.yml
release:
  preset: preset/release.yml
manifest:
  preset: preset/manifest.yml
build_cache:
  preset: preset/build_cache.yml
ci:
  forge:
    - gitlab
  image: docker.cr.pcfae.com/prplanit/stagefreight:latest-dev
forges:
  preset: preset/forges.yml
git:
  preset: preset/git.yml
metadata:
  category: infrastructure
  description:
    - Generates Kubernetes network and security policies from live cluster state.
    - Reads what a cluster actually does and emits the network/security policies to match, so policy is derived from observed traffic rather than written by hand.
  license: MIT
  org: PrPlanIT
  title: PolySieve
  topics:
    - kubernetes
    - network-policy
    - security
    - cilium
orgs:
  PrPlanIT:
    aliases:
      docker: prplanit
      ghcr: prplanit
      github: PrPlanIT
      handle: prplanit
    maintainer: PrPlanIT <precisionplanit@gmail.com>
preset_source:
  cache_policy: authoritative
  project_id: PrPlanIT/MaintenancePolicy
  provider: gitlab
  ref: 3f7d914f8c613003dc456dd939f81ac98e0b5b69
  repo_url: https://gitlab.prplanit.com
publish:
  preset: preset/publish-docker-binary.yml
registries:
  preset: preset/registries.yml
repos:
  github-mirror:
    forge: github
    project: PrPlanIT/PolySieve
    roles:
      - mirror
      - publish-origin
    sync:
      git: true
      releases: true
  primary:
    branches:
      default: main
    forge: gitlab
    project: PrPlanIT/PolySieve
    roles:
      - primary
    worktree: .
stencils:
  presets:
    - preset/stencils-base.yml
    - preset/stencils-status.yml
    - preset/stencils-docker.yml
    - preset/stencils-go.yml
test:
  preset: preset/test-go.yml
toolchains:
  preset: preset/toolchains.yml
