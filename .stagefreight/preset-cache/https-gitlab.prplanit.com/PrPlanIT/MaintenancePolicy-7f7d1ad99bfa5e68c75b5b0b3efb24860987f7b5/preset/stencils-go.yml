# Go add-on stencils — compose after stencils-core.yml for a Go repo.
# Both are native producers that resolve from the repo itself (go.mod / module path),
# so a Go repo needs no coordinates beyond declaring this preset.
stencils:
  go-version:   { type: go-version }
  go-reference: { type: go-reference }
